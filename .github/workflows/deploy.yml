services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - NODE_ENV=development
    volumes:
      - .:/app
      - /app/node_modules
    # Use dev command for hot reloading during local development
    command: npm run dev
